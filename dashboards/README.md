# Dashboards

Generated by `/mission`. Each run creates a static HTML snapshot at `cockpit-YYYY-MM-DD.html`.

The live dashboard runs on a local HTTP server during the mission session and updates in real-time as you interact.

Generated files are gitignored. Example files are kept for reference.
