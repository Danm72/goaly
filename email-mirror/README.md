# Email Mirror

Optional. Synced email threads from Gmail via `tools/email-sync/`.

Each thread is a markdown file with YAML frontmatter. Use for searching email history, cross-referencing with contacts, and loading context for lead screening and meeting prep.

## Setup
See `tools/email-sync/` for configuration.
