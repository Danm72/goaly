{
  "name": "@goaly/email-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Gmail email thread cache — sync to local markdown for qmd search",
  "type": "module",
  "scripts": {
    "sync": "tsx sync.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "p-queue": "^8.0.1",
    "p-retry": "^6.2.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "@types/js-yaml": "^4.0.9",
    "@types/turndown": "^5.0.5",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  }
}
