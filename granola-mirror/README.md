# Granola Mirror

Optional. Meeting transcripts synced from Granola via `tools/granola-sync/`.

Each meeting is a markdown file with AI summary, transcript, and action items. Used by coaching-prep and review-meeting skills.

## Setup
See `tools/granola-sync/` for configuration.
